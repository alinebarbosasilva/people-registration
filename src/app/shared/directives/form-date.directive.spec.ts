import { FormDateDirective } from './form-date.directive';

describe('FormDateDirective', () => {
  it('should create an instance', () => {
    const directive = new FormDateDirective();
    expect(directive).toBeTruthy();
  });
});
